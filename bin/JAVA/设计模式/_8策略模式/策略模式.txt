
策略模式：

定义：定义一组算法，将每个算法都封装起来，并且使它们之间可以互换


类图结构：
上下文(Context)：类，包含抽象策略的变量，定义一个委托抽象策略变量调用具体策略实现的方法
抽象策略(Strategy)：接口
具体策略(ConcreteStrategy)：实现类


优点：
1、算法自由切换
2、避免使用多重条件判断
3、扩展性好。增加一个策略只需实现抽象策略接口

缺点：
1、策略类增多
2、策略类对外暴露

应用情景：
1、多个类只在算法稍有不同
2、需自由切换算法





