

抽象工厂模式

定义：为创建一组相关或相互依赖的对象提供一个接口，而且无须指定它们的
具体类


优点：
1、封装性好，高层模块只需关心接口或抽象类，不需关心具体产品如何创建
2、具体工厂类内产品族的约束为非公开状态

缺点：
产品族扩展非常困难。若抽象工厂类增加创建新产品的抽象方法，则其的实现
类都要修改，违反开闭原则。注意：产品族扩展困难，而非产品等级，即纵向
扩展困难，横向扩展容易。

应用情景：
创建有约束关系的对象族，如不同操作系统的应用，通过抽象工厂模式屏蔽掉
操作系统对应用的影响